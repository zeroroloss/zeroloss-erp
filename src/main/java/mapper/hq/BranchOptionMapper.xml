<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.branchoption">
	<select id="selectBranchNameList" resultType="dto.hq.hr.BranchOptionDTO">
		select branch_code as branchCode, name as branchName from branch order by branch_code
	</select>
</mapper>